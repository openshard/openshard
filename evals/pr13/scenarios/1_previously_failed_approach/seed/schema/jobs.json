{
  "version": 1,
  "fields": [
    {
      "name": "name",
      "type": "str",
      "required": true,
      "help": "Unique job name."
    },
    {
      "name": "command",
      "type": "str",
      "required": true,
      "help": "Shell command the job runs."
    },
    {
      "name": "retries",
      "type": "int",
      "required": false,
      "default": 0,
      "help": "How many times to retry the job after a failure."
    }
  ]
}
