# Generated by scripts/gen_schema.py from schema/jobs.json.
# Regenerate with: python scripts/gen_schema.py
"""Job record schema (generated)."""

from __future__ import annotations

from typing import NamedTuple


class Field(NamedTuple):
    name: str
    kind: type
    required: bool
    default: object
    help: str


SCHEMA_VERSION = 1

FIELDS: tuple[Field, ...] = (
    Field('name', str, True, None, 'Unique job name.'),
    Field('command', str, True, None, 'Shell command the job runs.'),
    Field('retries', int, False, 0, 'How many times to retry the job after a failure.'),
)
